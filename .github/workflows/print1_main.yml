name: print
run-name: printName

on:
  push:
    branches:
      - main
      - feature/**

jobs:
  my_1st_job:
    name: simplePrintJob
    runs-on: Ranjeet1
    steps:
      - name: Name ko print krna
        shell: cmd
        run: echo thisIsMyFirstWorkflow
        
  
